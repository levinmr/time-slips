-title("Time-Slips info for: #{@sheet.name}")

=link_to "Create File to be imported", sheet_output_path(@sheet)

-@sheet.lines.each do |l|
  =render "line", :l => l
  
#-- At the bottom should be the option to create text file.
#-- link_to "Output file" output_sheet_path(@sheet)
